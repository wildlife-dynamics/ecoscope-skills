{
  "description": "Two hard invariants for Ecoscope workflow repos: a PreToolUse guard on git commit that denies staged real-data files and asks about fixtures with no committed generator, and a PostToolUse notice that the generated tree is stale after a spec.yaml edit.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/hook.sh\" sensitive_commit_guard.py"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/hook.sh\" spec_edited_notice.py"
          }
        ]
      }
    ]
  }
}
